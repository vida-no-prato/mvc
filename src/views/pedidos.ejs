<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meus Pedidos - Vida no Prato</title>
    <link rel="stylesheet" href="/css/vida-no-prato.css">
    <style>
        :root {
            --primary-green: #2e7d32;
            --accent-green: #4caf50;
            --page-bg: #f8fdf8;
            --card-bg: #ffffff;
            --border-color: #e8f5e9;
            --text-dark: #333;
            --text-light: #555;
        }

        body {
            background-color: var(--page-bg);
            color: var(--text-dark);
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 100px 20px 40px;
        }

        .page-title {
            text-align: center;
            color: var(--primary-green);
            margin-bottom: 40px;
            font-size: 2.5rem;
        }

        .btn-voltar-inicio {
            position: fixed;
            top: 20px;
            left: 30px;
            z-index: 100;
            background: linear-gradient(135deg, var(--accent-green), var(--primary-green));
            color: #fff;
            padding: 10px 18px;
            border-radius: 25px;
            font-weight: bold;
            text-decoration: none;
            box-shadow: 0 4px 15px rgba(76, 175, 80, 0.2);
            transition: all 0.3s ease;
        }

        .btn-voltar-inicio:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(76, 175, 80, 0.3);
        }

        .pedido-card {
            background: var(--card-bg);
            border-radius: 16px;
            margin-bottom: 25px;
            box-shadow: 0 8px 30px rgba(46, 125, 50, 0.08);
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }
        
        .pedido-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 35px rgba(46, 125, 50, 0.12);
        }

        .pedido-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background-color: var(--page-bg);
            border-bottom: 1px solid var(--border-color);
            flex-wrap: wrap;
        }

        .pedido-header h3 {
            font-size: 1.1rem;
            color: var(--primary-green);
            margin: 0;
        }

        .pedido-header span {
            font-size: 0.9rem;
            color: var(--text-light);
        }

        .status-badge {
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: bold;
            color: white;
            text-transform: capitalize;
        }

        .status-confirmado { background-color: #2980b9; }
        .status-preparando { background-color: #f39c12; }
        .status-entregando { background-color: #8e44ad; }
        .status-entregue { background-color: var(--accent-green); }
        .status-cancelado { background-color: #c0392b; }
        .status-pendente { background-color: #7f8c8d; }

        .pedido-body { padding: 20px; }

        .item-pedido {
            display: flex;
            align-items: center;
            gap: 15px;
            padding-bottom: 15px;
            margin-bottom: 15px;
            border-bottom: 1px solid #f0f0f0;
        }
        
        .item-pedido:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }

        .item-pedido img {
            width: 60px;
            height: 60px;
            border-radius: 8px;
            object-fit: cover;
            border: 1px solid var(--border-color);
        }

        .item-info { flex-grow: 1; }
        .item-info strong { display: block; color: var(--text-dark); }
        .item-info span { font-size: 0.9rem; color: var(--text-light); }
        .item-preco { font-weight: bold; color: var(--text-dark); }

        .pedido-footer {
            padding: 20px;
            text-align: right;
            font-size: 1.25rem;
            font-weight: bold;
            color: var(--primary-green);
            background-color: var(--page-bg);
            border-top: 1px solid var(--border-color);
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            background-color: var(--card-bg);
            border-radius: 16px;
        }
        .empty-state h2 {
             color: var(--primary-green);
        }
        .empty-state p {
            color: var(--text-light);
            margin-top: 10px;
        }

        @media (max-width: 600px) {
            .pedido-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }
            .item-pedido {
                flex-direction: column;
                align-items: flex-start;
                text-align: left;
            }
        }
    </style>
</head>
<body>
    <a href="/" class="btn-voltar-inicio">← Voltar ao Início</a>

    <div class="container">
        <h1 class="page-title">Histórico de Pedidos</h1>

        <% if (pedidos && pedidos.length > 0) { %>
            <% pedidos.forEach(pedido => { %>
                <div class="pedido-card">
                    <div class="pedido-header">
                        <div>
                            <h3>Pedido #<%= pedido.numero_pedido %></h3>
                            <span><%= pedido.data_formatada %></span>
                        </div>
                        <span class="status-badge status-<%= pedido.status %>"><%= pedido.status %></span>
                    </div>
                    <div class="pedido-body">
                        <% if (pedido.itens && pedido.itens.length > 0) { %>
                            <% pedido.itens.forEach(item => { %>
                                <div class="item-pedido">
                                    <img src="/images/uploads/<%= item.produto_imagem || '../default-plate.jpg' %>" alt="<%= item.produto_nome %>">
                                    <div class="item-info">
                                        <strong><%= item.produto_nome %></strong>
                                        <span><%= item.quantidade %> x R$ <%= Number(item.preco_unitario).toFixed(2).replace('.', ',') %></span>
                                    </div>
                                    <div class="item-preco">
                                        R$ <%= (item.quantidade * item.preco_unitario).toFixed(2).replace('.', ',') %>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <p>Itens do pedido não encontrados.</p>
                        <% } %>
                    </div>
                    <div class="pedido-footer">
                        Total do Pedido: R$ <%= Number(pedido.total).toFixed(2).replace('.', ',') %>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="empty-state">
                <h2>Você ainda não fez nenhum pedido.</h2>
                <p>Que tal explorar nosso cardápio e começar a sua jornada saudável?</p>
            </div>
        <% } %>
    </div>
</body>
</html>