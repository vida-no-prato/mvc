<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rastreando seu Pedido - Vida no Prato</title>
</head>
<body>
    <div class="tracking-container">
        <div class="map-card">
            <div class="map-header">
                <h1>Oba! Seu pedido está a caminho!</h1>
                <p>Número do Pedido: <strong>#<%= pedidoId %></strong></p>
            </div>
            <div class="map-area">
                <svg viewBox="0 0 300 150" xmlns="http://www.w3.org/2000/svg">
                    <path id="route" d="M 20 120 C 80 150, 150 10, 280 100" stroke="lightgray" stroke-width="3" fill="none" stroke-dasharray="5,5"/>
                </svg>
                <div class="point start">
                    <div class="icon">🏠</div>
                    <div class="label">Restaurante</div>
                </div>
                <div class="point end">
                    <div class="icon">📍</div>
                    <div class="label">Sua Casa</div>
                </div>
                <div class="delivery-icon">🛵</div>
            </div>
            <div class="status-footer">
                <p id="statusText">Seu pedido saiu para entrega...</p>
            </div>
        </div>
        <a href="/" class="btn-home">Voltar ao Início</a>
    </div>

    <style>
        @keyframes moveOnPath {
            0% { motion-offset: 0%; offset-distance: 0%; }
            100% { motion-offset: 100%; offset-distance: 100%; }
        }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background-color: #f8fdf8;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            box-sizing: border-box;
        }
        .tracking-container {
            text-align: center;
            animation: fadeIn 0.5s ease-out;
        }
        .map-card {
            background: #fff;
            border-radius: 24px;
            padding: 30px;
            box-shadow: 0 10px 40px rgba(46, 125, 50, 0.1);
            max-width: 500px;
            width: 100%;
            margin: 0 auto 20px auto;
            animation: slideUp 0.7s ease-out;
        }
        .map-header h1 {
            color: #2e7d32;
            font-size: 24px;
            font-weight: 600;
        }
        .map-header p {
            color: #555;
            margin-top: 5px;
            font-size: 14px;
        }
        .map-area {
            position: relative;
            margin: 30px 0;
            height: 150px;
        }
        .map-area svg {
            width: 100%;
            height: 100%;
        }
        .point {
            position: absolute;
            text-align: center;
        }
        .point .icon { font-size: 24px; }
        .point .label { font-size: 12px; color: #777; font-weight: 500; }
        .point.start { top: 105px; left: 5px; }
        .point.end { top: 85px; right: 5px; }
        
        .delivery-icon {
            position: absolute;
            font-size: 32px;
            /* Vincula o ícone ao caminho SVG para a animação */
            offset-path: path('M 20 120 C 80 150, 150 10, 280 100');
            motion-path: path('M 20 120 C 80 150, 150 10, 280 100');
            animation: moveOnPath 15s linear infinite;
        }
        .status-footer {
            background-color: #f8fdf8;
            padding: 15px;
            border-radius: 12px;
            color: #2e7d32;
            font-weight: 500;
        }
        .btn-home {
            text-decoration: none;
            padding: 12px 25px;
            border: 1px solid #ccc;
            border-radius: 25px;
            color: #555;
            background-color: #fff;
            font-weight: 500;
            transition: all 0.2s ease;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }
        .btn-home:hover {
            background-color: #f0f0f0;
            border-color: #bbb;
            transform: translateY(-2px);
        }
    </style>
</body>
</html>