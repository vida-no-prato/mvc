<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pagar com Pix</title>
    <style>
        /* --- ESTILOS COM ANIMAÇÕES --- */
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif; }
        body { background-color: #f4f7f6; display: flex; justify-content: center; align-items: center; height: 100vh; color: #333; }
        
        /* ANIMAÇÃO DE ENTRADA DO CARD */
        .pix-container {
            background-color: #ffffff;
            border-radius: 16px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 8px 30px rgba(0,0,0,0.1);
            max-width: 400px;
            width: 90%;
            animation: slideUp 0.6s ease-out forwards;
        }

        .pix-title { font-size: 24px; font-weight: 600; margin-bottom: 10px; }
        .pix-amount { font-size: 32px; font-weight: 700; color: #2e7d32; margin-bottom: 25px; }
        
        /* ANIMAÇÃO SUTIL NO QR CODE */
        .qr-code-image {
            width: 250px;
            height: 250px;
            margin: 0 auto 20px auto;
            border: 1px solid #eee;
            border-radius: 8px;
            display: block;
            animation: pulse 2s infinite ease-in-out;
        }

        .pix-copy-paste-label { font-weight: 500; margin-bottom: 10px; }
        .pix-copy-paste-code { background-color: #f0f0f0; border: 1px solid #ddd; border-radius: 8px; padding: 12px; word-wrap: break-word; font-family: monospace; font-size: 14px; margin-bottom: 25px; color: #555; }
        .btn-back { text-decoration: none; padding: 12px 25px; border: 1px solid #ccc; border-radius: 8px; color: #555; background-color: #fff; font-weight: 500; transition: all 0.2s ease; }
        .btn-back:hover { background-color: #f0f0f0; border-color: #bbb; transform: translateY(-2px); }

        /* DEFINIÇÃO DAS ANIMAÇÕES */
        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.03); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body>
    <div class="pix-container">
        <h1 class="pix-title">Pague com Pix para finalizar</h1>
        <p class="pix-amount">R$ <%= total %></p>
        <img src="/images/qrcode.png" alt="QR Code para pagamento Pix" class="qr-code-image">
        <p class="pix-copy-paste-label">Ou use o Pix Copia e Cola:</p>
        <div class="pix-copy-paste-code">
            00020126330014br.gov.bcb.pix01111234567890123020...
        </div>
        <a href="/" class="btn-back">Voltar para o Início</a>
    </div>
</body>
</html>